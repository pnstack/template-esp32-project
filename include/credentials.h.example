#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Credentials (Optional - can be configured via web interface)
// Uncomment and fill these in for hardcoded credentials
// #define WIFI_SSID "YourWiFiNetworkName"
// #define WIFI_PASSWORD "YourWiFiPassword"

// HTTP API Endpoint (Optional - for sending data)
// #define API_ENDPOINT "http://your-server.com/api/data"
// #define API_KEY "your-api-key-here"

// OTA Update Credentials (Optional - change from defaults)
// #define OTA_HOSTNAME "my-esp32-device"
// #define OTA_PASSWORD "my-secure-password"

// Notes:
// 1. Copy this file to credentials.h and fill in your actual credentials
// 2. The credentials.h file is in .gitignore and won't be committed
// 3. WiFi can also be configured via the web interface at runtime
// 4. Keep this file secure and never commit it to version control

#endif // CREDENTIALS_H
